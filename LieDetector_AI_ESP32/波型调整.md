要调整显示的秒数，核心在于修改 **`WAVE_LEN`** 这个变量。

在代码的第 **31 行** 左右，你可以看到这个定义。

### 修改方法：

1. **如果你想显示 2 秒：**
   将 `WAVE_LEN` 设置为 **100**。
   *计算原理：* 你的 STM32 代码里最后有 `delay(20)`，意味着每秒发送约 50 次数据。$50次/秒 \times 2秒 = 100$ 个数据点。

```python
# --- 2. 数据缓冲区 (修改这里来调秒数) ---
WAVE_LEN = 100  # 约 2 秒的数据点 (50Hz * 2s)
waveform_queue = deque([0]*WAVE_LEN, maxlen=WAVE_LEN)
```

### 手动调优指南：

*   **变快/变慢**：
    *   `WAVE_LEN` **越小**（如 50），波形在屏幕上跑得**越快**，看起来越实时，但只能看到极短时间的历史。
    *   `WAVE_LEN` **越大**（如 500），波形跑得**越慢**，你能看到更长的心跳历史。

*   **波形高矮（振幅）**：
    如果你觉得波形跳动不够明显，可以修改 `draw_waveform` 函数里的缩放系数（大约在第 **85 行**）：
    ```python
    # 将 0.6 改大（如 1.5），波形会变高；改小（如 0.3），波形会变矮
    y = origin_y - int(val * 1.5) 
    ```

*   **波形颜色灵敏度**：
    如果你想让心跳稍有起伏就变红，可以改第 **93 行**：
    ```python
    # 将 60 改小（如 30），波形会更容易变红
    color = (0, 255, 0) if abs(data[i]) < 30 else (0, 0, 255)
    ```

### 总结：
直接把 **`WAVE_LEN = 250`** 改成 **`WAVE_LEN = 100`**，你就能看到屏幕上滚动的 2 秒心率波形了！