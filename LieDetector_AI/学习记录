# 📝 学习档案：基于 RTX 5080 的实时数字测谎实验室开发记录

## 1. 项目愿景 (Project Vision)
利用 **STM32** 采集物理生理信号（心率/脉搏波形），结合 **RTX 5080** 顶级算力进行 AI 微表情分析，构建一套“生理+心理”双维度的数字测谎系统，并实时输出控制反馈（电机/视觉警报）。

## 2. 核心挑战：顶配硬件的“孤独”
*   **硬件**：NVIDIA RTX 5080 (Blackwell 架构, `sm_120`)。
*   **软件环境**：Ubuntu Linux + PyTorch 2.12 (Dev版) + CUDA 12.8。
*   **痛点**：5080 架构太新，导致传统的 AI 框架（如 TensorFlow）出现指令集不兼容（`INVALID_PTX` 报错）。所有的成熟高层库（DeepFace, Py-Feat）在面对这种“超前”驱动环境时，都暴露出了底层的路径管理、显存对齐和依赖冲突问题。

## 3. 技术进化的四个阶段

### 第一阶段：集成派（DeepFace 尝试）
*   **思路**：使用现成的 DeepFace 库，快速实现情绪识别。
*   **挫折**：DeepFace 强绑定 TensorFlow，而官方 TF 不支持 5080 的 `sm_120` 架构。
*   **教训**：顶配显卡用户必须拥抱 **PyTorch 原生生态**，远离过时的框架。

### 第二阶段：专业派（Py-Feat 攻坚）
*   **思路**：利用 FACS（面部动作单元）理论，通过监控眉头（AU04）、恐惧感进行深度测谎。
*   **挫折**：
    1.  **版本断层**：Scipy 升级导致函数被删（`simps` 报错）。
    2.  **安全限制**：PyTorch 2.12 默认禁止加载自定义类（`weights_only` 报错）。
    3.  **设备打架**：库内部代码不规范，导致 Tensor 在 CPU 和 GPU 之间打架（`Two devices` 报错）。
*   **解决**：通过“猴子补丁”拦截联网请求、强制注入全局函数、手动搬运模型到显存。

### 第三阶段：极简主义（原子化重构）
*   **思路**：抛弃所有黑盒封装库。直接用 PyTorch 加载 `.pt` 权重，自己写推理流水线。
*   **突破**：绕过了 `Py-Feat` 内部混乱的文件读取逻辑，直接把摄像头矩阵（Numpy）喂进 5080 显存。
*   **成果**：彻底消灭了所有 Device Mismatch 和 Read 错误。

### 第四阶段：全栈闭环（最终形态）
*   **实现**：STM32 通过串口发送“波形+数值”双格式数据 -> Python 异步线程解析 -> 5080 毫秒级情绪推理 -> OpenCV 实时绘制 2 秒心电图波形。
*   **架构**：`OpenCV (找脸) -> PyTorch (算情绪) -> Serial (读心率) -> Logic (综合判定)`。

## 4. 技术锦囊 (Key Takeaways)

1.  **5080 的适配核心**：面对 Blackwell 架构，手动安装离线 `.whl` 包（如 `cu128` 版本）是唯一出路。
2.  **显存对齐**：在处理跨设备报错时，`torch.inference_mode()` 配合手写 `to(device)` 比任何自动化工具都靠谱。
3.  **离线思维**：大型 AI 库的自动下载机制极不可靠。**“手动下载模型文件 + 强行指定路径”**是工业级项目的标准操作。
4.  **底层流水线优势**：手动解开 `检测 -> 对齐 -> 识别` 的步骤，不仅能排错，还能获得比封装库快 5-10 倍的帧率（5080 推理延迟控制在了 2ms 以内）。

## 5. 最终成果展示
*   **视觉**：流畅的 60FPS 视频流 + 2秒动态心律波形。
*   **逻辑**：心率阈值 + 微表情（恐惧/蔑视）综合触发测谎警报。
*   **环境**：极致纯净，全电脑只认一个 `.pt` 模型，无任何冗余缓存。

---
**结语**：
这次开发不仅是写代码，更是一次对 **Linux 环境工程、PyTorch 底层架构、以及跨设备通信协议** 的深度探索。在顶级硬件面前，最强大的工具往往是**最简单的底层原生代码**。
